import { billespaceServices } from './billespace-services';

describe('billespaceServices', () => {
  it('should work', () => {
    expect(billespaceServices()).toEqual('billespace-services');
  });
});
